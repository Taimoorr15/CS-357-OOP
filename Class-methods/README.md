# Class-methods
A class method is a type of method within a class that operates on the class itself, rather than on a specific instance (object) of that class. In many object-oriented programming languages, such as Python
