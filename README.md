# Private-function
DIFFERENCE BETWEEN PUBLIC AND PRIVATE FUNCTIONS: PUBLIC FUNCTION:Can be accessed from anywhere (inside or outside the class)Defined normally with def function_name(self):Most functions you create are public by default.   PRIVATE FUNCTION: Can be accessed only from within the class. Defined with double underscore prefix like def __function_name(slf)
